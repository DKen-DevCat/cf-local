# Phase 4c: 仕上げ — タスク履歴 (完了 2026-05-03)

ブランチ: `feat/phase-4c-polish`（develop @ `0eae0e7` 起点、PR #12 で develop に merge 済）

設計: [`../design/polish-2026-05-03.md`](../design/polish-2026-05-03.md)

完了レビュー: `/phase-review` 4 軸並列で REV-1 (plan.md / design doc 完了マーク) + REV-2 (validate.isHTTPTokenName を renderer の allow-list に統一) を採用 + 修正済 (commit `9386799`)。`/security-review` は 0 件。

実機検証: リポジトリルート `check-phase-4c.md` に V-1 〜 V-9 の手順 + 結果を記録。docker compose up + AWS CLI + Terraform + curl + vegeta + JSON ログ全 PASS (2026-05-03)。

## kickoff 前相談で確定した方針

- [x] **A-1 エラー互換**: 中程度 (CloudFront `<ErrorResponse>` 形式 + 主要 Code)
- [x] **A-2 ログ**: `log/slog` 人間可読 default + `CF_LOCAL_LOG_FORMAT=json` で JSON 切替
- [x] **A-3 デバッグ UI**: 作らない (out)
- [x] **D ResponseHeadersPolicy**: `CustomHeadersConfig` + `CorsConfig` のみ実装

## スコープ

- [x] **4c-1**: ResponseHeadersPolicy CRUD (`aws_cloudfront_response_headers_policy`) — `internal/api/responseheaderspolicy/` 新規 (store + bolt + handler + id) + `internal/api/xml/response_headers_policy{,_convert}.go`。全 5 sub-config を accept + 永続化、`SecurityHeadersConfig` / `ServerTimingHeadersConfig` / `RemoveHeadersConfig` は slog.Warn で警告 (4c-6 で log.Printf から置換)。Bolt bucket `response_headers_policies` 追加、reloader.Trigger 配線、route `POST/GET/PUT/DELETE /2020-05-31/response-headers-policy{/id}`
- [x] **4c-2**: distribution との関連付け (cache behavior 経由で `add_header` directive を nginx.conf に注入) — `internal/config/loader.go` (LoadResult.ResponseHeadersPolicies 追加) / `internal/nginx/{renderer,conf,response_headers}.go` (CustomHeaders + Cors のみ反映、Override=true で `proxy_hide_header` 注入、unsafe header name/value は drop) / `cmd/cf-local/main.go` snapshot で rhpStore 配線。CRLF / `"` injection guard 込みのテーブル駆動テスト 14 ケース + render-end-to-end テスト 2 件 PASS
- [x] **4c-3**: AWS 互換エラー形式 `<ErrorResponse><Error><Code>...</Code><Message>...</Message></Error></ErrorResponse>` の共通ヘルパー + 既存ハンドラ移行 — `internal/api/xml/codes.go` 新規 (typed `Code` 定数 + `statusForCode` + `WriteError` / `WriteInternalError`)。14 コード定数 (NoSuch*5, *AlreadyExists*4 + EntityAlreadyExists, InvalidArgument, MalformedXML, IllegalUpdate, InvalidIfMatchVersion, PreconditionFailed, InternalError) を追加。全 6 ハンドラ (cachepolicy / distribution / originrequestpolicy / responseheaderspolicy / invalidation / tagging) を `WriteError` / `WriteInternalError` に移行、`WriteXMLError` は escape hatch として保持。codes_test.go で status マッピング 18 ケース + envelope 2 ケース PASS
- [x] **4c-4** (BL-IM1): managed CachePolicy `IllegalUpdate` AWS 正規コード確認 — `aws-sdk-go-v2 v1.62.0/deserializers.go` で UpdateCachePolicy / DeleteCachePolicy が `IllegalUpdate` を valid response code として smithy spec 由来で登録していることを確認。SDK が解釈可能なコードを返している以上 Provider/CLI 側の regression は無い (実 AWS 挙動の正確な確認は phase-5 で差し替え検討)。handler のコメントを更新
- [x] **4c-5** (BL-OB1): HTTP request log middleware — `internal/api/middleware/log.go` 新規 (RequestLog wraps mux、recordingResponseWriter で status + bytes 捕捉、`X-Cf-Local-Request-Id` を response header と context に伝播)。`internal/api/server.go` に `Logger` field 追加 + Run 時に middleware を挿入。`cmd/cf-local/main.go` で `configureSlog(CF_LOCAL_LOG_FORMAT)` (default text / `json` 切替) を初期化。テスト 7 件 (attrs / 200 default / context propagation / error status / double WriteHeader / IDGen fallback / nil context) PASS
- [x] **4c-6**: 既存 `log.Printf` を slog に統一 — `responseheaderspolicy/handler.go` の `log.Printf` → `slog.Warn` (event=response_headers_policy_subconfig_ignored)、`internal/nginx/reloader.go` の `fmt.Fprintf(stdout, ...)` を slog 呼び出しに置換 (`reloader_rendered` info / `reloader_render_failed` 等の error)、`Reloader` 構造体の `stdout io.Writer` を `logger *slog.Logger` に置換 + reloader_test.go を slog buffer 検証に更新、`cmd/cf-local/main.go` の `log.Fatalf` を `slog.Error + os.Exit(1)` に置換、起動 banner の reloader 行を `slog.Info("reloader_started")` に置換 (config-dir / cache-policy 数等の boot summary は stdout に残す: 一回限りの banner なので runtime log と分離)。`grep '"log"'` で imports ヒットゼロ確認
- [x] **4c-7** (BL-NX1): inner location の unix socket 化 — `internal/nginx/conf.go` の renderer を 2 server block 構成に分割 (公開 `:8080` には outer のみ / 内部 `unix:/run/cf-local-inner.sock` に inner)、`upstream self` を unix socket 経由に置換、`allow 127.0.0.1; deny all;` を撤去 (構造的にバイパス不可)。`nginx/scripts/reload-entrypoint.sh` で `umask 000` を設定して master(root) が作成する socket を mode 0666 にし worker(nginx user) から connect 可能に。golden fixture 3 件 (min/multi-policy/ae-flags) 更新 + `TestRender_InnerServerOnUnixSocket` 追加で公開 server に `location /_cf_inner_` が無いことを assert。`docs/ttl.md` / `docs/limitations.md` を新構成に追従
- [x] **4c-8** (BL-NX2): stress test (vegeta 1000 RPS / 1 分) — `tests/stress/` 新規 (run.sh harness + README + baseline.md + .gitignore)。harness は vegeta CLI ローカル install / `peterevans/vegeta` Docker fallback の両対応、warm-up 5 GET → attack → summary.txt + histogram.txt 出力。実機検証 V-8 で post-4c-7 baseline を取得 (β endpoint 200 RPS / 100% Success / p99=26ms / :8080 は origin 不在のため 502 — `tests/stress/baseline.md` 反映)
- [x] **4c-9** (BL-LD1): Go loader sanitize — phase-3 file-based loader が持っていた behavior enum + items 一貫性 + name char allow-list の検証を、phase-4a の AWS XML handler 経由 path にも適用。`internal/api/validate/policy.go` 新規 (CachePolicyConfig / OriginRequestPolicyConfig / ResponseHeadersPolicyConfig)、3 ハンドラの `decodeConfig` で wrapper.ToSDK() 直後に `validate.*Config(cfg)` を呼び失敗時は `awsxml.WriteError(CodeInvalidArgument)`。allow-list は phase-3 loader と統一 (`A-Za-z0-9._-`)、ResponseHeadersPolicy の Custom/Remove header 名は REV-2 修正後に renderer と同じ `[A-Za-z0-9-]` に強化 (silent drop 解消、API boundary で explicit エラー)。policy_test.go 41 ケース PASS
- [x] **4c-10**: `docs/limitations.md` 完成 — phase-3/4a/4b/4c で判明した未対応項目をすべて反映 (CachePolicy の AWS spec 完全網羅 / CacheBehavior PathPattern / ResponseHeadersPolicy / Invalidation wildcard / ETag 関連 / 環境固有差分の各 section を更新)
- [x] **4c-11**: 積みタスク BL-W1/W2/IV1/IV2/PP1/NX3 (+ BL-IM1/NX1/NX2/LD1/OB1/RV1/RV2) を `docs/limitations.md` の「積みタスク (backlog) 一覧」表として 1 段落に集約。完了済 (BL-NX1/LD1/OB1/IM1) は **完了** マークで識別、未着手は条件付きで残す

## レビュー対応 (REV-1, REV-2 — commit 9386799)

- [x] **REV-1** (Critical): `.claude/plan.md` ステータス表 + §phase-4c 完了条件 + design doc 完了条件を `[x]` に統一、design doc 末尾に「Phase 完了時メモ」追記 (CLAUDE.md §進捗の記録 違反を解消)
- [x] **REV-2** (Important): `internal/api/validate/policy.go` の `isHTTPTokenName` を renderer の `[A-Za-z0-9-]` に統一 (silent drop 解消、API boundary で explicit エラー — 4c-9 設計意図に揃える)

## 完了消化 BL

- BL-NX1 → 4c-7 で消化
- BL-NX2 → 4c-8 で消化 (実機 baseline 取得は V-8 で実施)
- BL-LD1 → 4c-9 で消化
- BL-IM1 → 4c-4 で SDK 解釈互換確認 (実 AWS 挙動の差し替えは phase-5)
- BL-OB1 → 4c-5 で消化

## 引き継ぎ

設計 doc §「Phase 完了時メモ」を参照。要約:

- 残 BL: BL-W1 / BL-W2 / BL-IV1 / BL-IV2 / BL-PP1 / BL-NX3 / BL-RV1 / BL-RV2 (`docs/limitations.md` §積みタスク一覧で indexed)
- ETag `If-Match` strict 検証 (`InvalidIfMatchVersion` 412) は permissive のまま phase-5 持ち越し
- ResponseHeadersPolicy の Security / ServerTiming / RemoveHeaders 3 系統 nginx 注入は要望次第で phase-5 候補
- 実機検証 V-5 で発見した運用注意: API mutation 後は reloader が `policies.json` を BoltDB-only に上書き、α/β tests を回す前は `docker compose down -v` 必要 (phase-4a 4a-10 挙動)
- DESIGN.md 更新候補: HTTP request log の構造化フィールド (BL-OB1 完了) を反映
