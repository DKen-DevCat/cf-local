# Phase 3: Invalidation API + 設定ファイル方式 — タスク履歴 (完了 2026-04-30)

ブランチ: `feat/phase-3-invalidation-config`（develop @ `e60787c` 起点）

設計: [`../design/phase-3-invalidation-config-2026-04-30.md`](../design/phase-3-invalidation-config-2026-04-30.md)

完了レビュー: 同設計 doc の §「Phase 完了時メモ」

### 着手前決定事項 (kickoff で確定)

- [x] A. 設定ファイル配置: リソース別ディレクトリ分割 (`./cf-local/distributions/*.json` + `./cf-local/cache-policies/*.json`)、AWS SDK Go v2 型を JSON marshal した形
- [x] B. nginx reload 戦略: 共有 named volume + nginx container 内 inotify sidecar (atomic rename + 500ms debounce)、Phase 3 では Control Plane 側 watch なし
- [x] C. `ngx_cache_purge`: `nginx-modules/ngx_cache_purge` を `--with-compat` で dynamic module 化、3-2 spike で実機確認、NG なら debian source build (Plan B)
- [x] Q1. List 型は flat array 簡略化 (loader が `Quantity` 自動算出)
- [x] Q2. Phase 3 は `distributions/` 1 ファイル限定、複数は Phase 4-A 送り
- [x] Q3. Cookie/QueryString の 4 behavior (`none` / `whitelist` / `allExcept` / `all`) を njs 全対応
- [x] Q4. `EnableAcceptEncodingGzip` / `EnableAcceptEncodingBrotli` 独立フラグ化

### コアスコープ

- [x] 3-1 設定ファイルスキーマ確定 + `docs/config-schema.md` 初版 (A 群 / B 群 / C 群 三分類)
- [x] 3-1a njs `cache_key.js` を 4 behavior 対応 (`none` / `whitelist` / `allExcept` / `all`) — T20-T25 で検証
- [x] 3-1b njs `cache_key.js` を `EnableAcceptEncodingGzip` / `EnableAcceptEncodingBrotli` 独立フラグ化 — T26-T30 で検証
- [x] 3-1c 内部 `policies.json` schema 移行 (PascalCase + 3-1a/b 反映) + `ttl.js` の連動修正
- [x] 3-2 spike: `nginx-modules/ngx_cache_purge` v2.5.5 を `--with-compat` で dynamic module ビルド検証 — PASS (`nginx/spike/README.md`)
- [x] 3-2 本実装 (A.1): multi-stage Dockerfile + ngx_cache_purge v2.5.5 dynamic module + nginx.conf に `load_module` 追加 (実発火 location は A.5 で追加、α regression PASS)
- [x] 3-3 Go 基盤 (`cmd/cf-local/main.go`) + `internal/config` loader (TDD) — A.3a/A.3b の 2 コミットで完了。独自 Schema 型 (flat array) → AWS SDK Go v2 cloudfront/types 変換 + Phase 3 制約 (distributions 1 ファイル限定) + cross-ref validation。テーブル駆動テストで Happy 6 + Error 14 ケース。REV-7 (MinTTL 負値 / MinTTL > MaxTTL) は Go loader 側でも fail-fast (njs 側は別タスクで残置)
- [x] 3-4 / A.4 `internal/nginx` renderer + Control Plane 常駐化 + named volume 経由配信 (3-4 + 3-5 残)
    - 詳細: 設計ドキュメント §「A.4 詳細設計 (3-4 + 3-5 残)」
    - [x] A.4.0 golden test fixture 配置 (`internal/nginx/testdata/{min,multi-policy,ae-flags,disabled}/{cache-policies,distributions,*.json,*.conf}`) — TDD の input/expected を先に。loader smoke で全 4 ケース読み込み OK
    - [x] A.4.1 `internal/nginx/renderer.go` skeleton + `Render(*config.LoadResult) (*Output, error)` 型定義 + `render_test.go` table-driven test (全 4 ケース unimplemented で FAIL することを確認、gofmt/go vet クリーン)
    - [x] A.4.2 policies.json 生成 (intermediate struct で flatten + omitempty marshal) + 全 4 ケース policies.json golden PASS。subtest は cf-local.conf 側で依然 FAIL なので最終 PASS は A.4.6 で達成
    - [x] A.4.3 nginx.conf 生成: upstream + DefaultCacheBehavior の outer/inner ペア + `min` / `ae-flags` golden PASS。ついでに A.4.6 disabled 分岐も Render dispatch のついでに同コミットで実装 (disabled comment 1 行返す)
    - [x] A.4.4 PathPattern → location 変換 + sanitize + `multi-policy` golden PASS。`pathPatternToLocation` で `/api/*` → `/api/`、`*` → `/`。inner dedup は sanitized policy id ベース (同 policy 複数 behavior で 1 つ)
    - [x] A.4.5 loader 側 PathPattern 受理規則 (prefix `/path/*` / `*` のみ) + `pathpattern-reject` テスト 4 ケース追加
    - [x] A.4.6 `disabled` ケース (Distribution.Enabled=false で空出力) + golden PASS — A.4.3 で先取り実装
    - [x] A.4.7 `internal/nginx/writer.go` の `WriteAtomic` 実装 + unit test (3-5 残)。tmp file (`.<name>.tmp`) + O_TRUNC + fsync + rename(2)。stale tmp / overwrite / path separator reject / empty name / nonexistent dir のテスト 6 ケース PASS
    - [x] A.4.8 `cmd/cf-local/main.go` を renderer 配線形に書き直し
    - [x] A.4.9〜12 image / compose / 設定の renderer 移行を 1 commit でまとめて実施 (POLICIES_PATH 切替 / β test endpoint 分離 / repo root Dockerfile 新規 / docker-compose 改修 / cf-local/ 配置)
    - [x] A.4.13 α regression PASS 確認 — 新構成で Phase 1 α 4/4 + Phase 2 α 7/7 PASS。発見した β only loose ends 2 件 (test js_import 落ち / test policies 引き継ぎ落ち) を併せて修正
- [x] 3-5 spike: 共有 named volume + inotify sidecar の reload 経路検証 — PASS (debounce は busybox 制約で 1 秒に確定)
- [x] 3-5 本実装 (A.2): sidecar スクリプト配置 + Dockerfile に inotify-tools + nginx.conf を `include /etc/nginx/cf-local/*.conf` 化
- [x] 3-5 残: Control Plane で atomic rename 実装 — A.4.7 + A.4.8 で吸収
- [x] 3-2 残 + 3-6 + 3-7 (A.5): MVP は「default policy / 空 headers/cookies/queries / AE=identity の 1 variant のみ purge、完全一致、同期実行、cf-local 独自 JSON `{"paths":[]}`」
    - [x] A.5.1 renderer の `_cf_purge` location を cache_key 経由に修正 + `cache_key.js` の `forNginx` に `cf_purge_uri` override 対応 + 全 fixture golden 更新
    - [x] A.5.2 `internal/api/invalidation/` package + handler skeleton + table-driven test 16 ケース PASS
    - [x] A.5.3 `cmd/cf-local/main.go` を ListenAndServe 化 + `:4566` listen + graceful shutdown
    - [x] A.5.4 nginx 内部 purge への HTTP client 実装 (`NginxPurger`) + 200/204/404/412 を success として扱う + 11 ケース PASS。control plane → nginx の private CIDR allow を renderer 出力に追加
    - [x] A.5.5 α 統合テスト 4 ケース PASS。発見した bug fix: `Accept-Encoding: identity` 明示で Go の auto-gzip 付与による cache_key variant ズレを回避
    - [x] A.5.6 docs 整備 (`docs/limitations.md` Invalidation 書き直し / `docs/invalidation-api.md` 新規 / `docs/config-schema.md` 関連 doc link 追加)
- [x] 3-8 `examples/` 拡充 + CMS 連携ドキュメント — `examples/nextjs-basic/README.md` を Phase 3 2-service 構成 + invalidation 項に更新 + `docs/invalidation-api.md` に CMS webhook 連携 section 追加

### Phase 4-B / 後半に持ち越すタスク (Phase 3 で起こすだけ、本フェーズでは触らない)

- [ ] 後半-1 AWS API 互換 (`POST /2020-05-31/distribution/{Id}/invalidation` の XML 形式)
- [ ] 後半-2 wildcard サポート (`/foo/*` 等)
- [ ] 後半-3 multi-variant invalidation (cookie/header/AE 違いの全 slot を消す)
- [ ] 後半-4 非同期実行 + status (`InProgress`/`Completed`) + `GetInvalidation`/`ListInvalidations`
- [ ] 後半-5 invalidation 履歴の永続化 (BoltDB)

### Phase 3 review 対応 (A.4 着地レビューより)

- [x] SEC-1 設定文字列 allow-list + comment sanitize defense-in-depth
- [x] REV-2 同 sanitized policy id で異 TargetOriginId を error 化
- [x] REV-3+10b `inotifywait` を -m monitor mode + stderr を流す
- [x] REV-5 CachePolicyId 空を fail-fast + docs に必須表記
- [x] REV-6 `WriteAtomic` parent dir fsync 追加
- [x] docs REV-1 / REV-4/8 を docs に追記

### Phase 2 review からの繰越し (3-Rv) — 全消化

- [x] REV-7 `getPolicyTtl` validation (sanitizeTtl 追加、broken policy 2 件 + 検証 4 ケース)
- [x] REV-10 TTL error path observability (A.0 で先行消化)
- [x] REV-3 多 policy 対応で `_test-ttl-clamp` を α テスト実 location に
- [x] REV-5 AT02 sleep を 3.5s + wall-clock log
- [x] REV-9 testserver malformed query で 400 + unit test 4 ケース
- [x] REV-14 `cache_control.js` `max-age=` (値空) drop の test 追加

### 完了条件 (達成)

- [x] 全 β / α テスト PASS、Phase 1〜2 の α regression 維持
- [x] `gofmt` / `go vet` クリーン
- [x] 手動: 設定ファイル → docker compose up → HIT → `POST /_invalidate` → MISS が curl で確認できる
- [x] 「Phase 完了時メモ」を設計ドキュメント末尾に追記
